/* export interface HotelData {
    hotelname: string;
}

export type User = {
    email: string;
    password: string;
}

export type AuthState = {
    loggedIn: boolean;
    user: User;
    loading: boolean;
} */